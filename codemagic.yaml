workflows:
  flet-android-workflow:
    name: Flet Android APK Build
    max_build_duration: 15
    instance_type: mac_mini_m1
    environment:
      groups:
        - python
      vars:
        FLET_VERSION: "0.85.3"
    scripts:
      - name: Initialize Environment and Dependencies
        script: |
          pip install flet==$FLET_VERSION mysql-connector-python
      - name: Execute Flet Native Android Build
        script: |
          # Bundles layout files dynamically
          mkdir -p src
          mv -f mobile_monitor.py src/main.py
          mv -f flet.yaml src/
          
          # ✅ FIX: Copies the requirements tracker into the source folder 
          # so the builder packs the MySQL driver directly into the phone app
          mv -f requirements.txt src/
          
          # Automatically passes the yes confirmation hook
          yes | flet build apk src
    artifacts:
      - src/build/apk/*.apk
